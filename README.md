U only need launcher.py groovebox.py groovebox.sh for all 3 majr OSs
With strong geometric interphase and phase lock properties, infinitely self-remixing and objective harmonic design pathway omniscience

Fractallization Engine, Tensor of Equation of Reality & Geometric Phase-Locked Wavefield Engine
An experimental electronic music production and high-energy mathematical modeling environment. EQR treats sound synthesis as a physical wavefield where user compositions act as carrier waves, irrational seeds serve as geometric anchors, and Euclidean rhythm generators enforce total phase coherence.Core Architectural Features1. Phase-Locked Loop (PLL) Wavefield EngineInstead of traditional, destructive procedural randomizers that wipe out user input, EQR uses a phase-locked wavefield model:Carrier Waves: Your manual compositions and step sequences form the baseline structural matrix.Seed Control Points: Irrational numbers ($e$, $\pi$, or the Meum constant $\approx 1.1975807343$) act as deterministic harmonic modulation anchors.Euclidean Coherence: Euclidean rhythm distribution geometry binds the step grid into complete phase alignment with the active seed without erasing user data.Zero-Seed Baseline: Setting the seed input to 0 operates as the clean baseline phase origin, allowing raw user compositions to run uninhibited.2. Decimal-Friendly Interval PlayAll primary transport and sequencing parameters are built using floating-point precision components (QDoubleSpinBox), allowing smooth micro-tonal adjustments and fractional step control:Tempo: Decimal-friendly fractional BPM tuning.Sequence Lengths & Rows: Fluid control over step divisions across the 48-operator ecosystem.3. Non-Redundant Patch Bay RoutingTo prevent closed feedback loops, dead-end nodes, or conflicting duplicate sources, the advanced modular patch bay integrates an irrational-seed coordinate mapping algorithm. It automatically wires a clean, non-redundant feedforward signal flow across all 48 operators.Component OverviewUIComponentManager: Manages the top-level transport bar, decimal-friendly transport controls (spin_tempo, spin_seq_length, spin_row_count), EQR/Fractalizer modulation sliders, and the irrational seed input (input_seed_val, defaulting to 0).PhaseLockedWavefieldEngine: Handles the deterministic hash generation from irrational seeds, executes the Phase-Locked Loop randomization logic, and computes non-redundant patch bay connections.SequencerGridManager: Dynamically renders step buttons, amplitude readouts, and real-time UI updates across active operator memory buffers.Usage & WorkflowComposition Mode (Seed = 0): Leave the seed input at 0 to build your base arrangements, melodies, and step grids as uninhibited carrier waves.Phase-Lock Modulation (Irrational Seed > 0): Input a custom irrational seed (such as the Meum constant 1.1975807343, $\pi$, or $e$) and trigger the 🎲 Phase-Locked Harmonic Randomizer.Coherent Wave Interaction: The engine evaluates the seed against your existing composition via wave superposition and Euclidean phase-locking, instantly syncing the 48-operator ecosystem into a unified acoustic interference pattern.
